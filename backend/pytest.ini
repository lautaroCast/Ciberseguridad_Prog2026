[pytest]
testpaths = tests
pythonpath = . ../database
; The backend container runs with read_only: true (Módulo 9) and only
; /tmp is writable (tmpfs) — the default ./.pytest_cache would otherwise
; fail to write on a fresh container/image.
cache_dir = /tmp/.pytest_cache

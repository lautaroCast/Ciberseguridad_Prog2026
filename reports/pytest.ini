[pytest]
testpaths = tests
pythonpath = .
; The reports container runs with read_only: true (Módulo 9) and only
; /tmp is writable (tmpfs) — the default ./.pytest_cache would otherwise
; fail to write on every run.
cache_dir = /tmp/.pytest_cache
